
#include <Structures/LinkedList.h>
#include <iostream>
#include <Structures/Graph.h>

int main()
{
    Graph graph;
    graph.addVertex(2);
    graph.addVertex(3);
    graph.addVertex(5);
    graph.addVertex(7);
    graph.addVertex(9);
    graph.addVertex(11);
    graph.addVertex(13);
    return 0;
}