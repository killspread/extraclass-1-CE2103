
#include <Structures/LinkedList.h>
#include <iostream>
#include <Structures/Graph.h>

int main()
{
    Graph graph;
    graph.addVertex(0);
    graph.addVertex(1);
    graph.addVertex(2);
    graph.addVertex(3);
    graph.addVertex(4);
    return 0;
}